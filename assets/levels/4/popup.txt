<p class="uk-text-bold">Level 1:</p> Movement: <code>Player.walk();</code>
<p class="uk-text-bold">Level 2:</p> Attack: <code>Player.attack();</code><br> Next field: <code>Player.getNext();</code><br> Branching: <code>if(function() == "something"){ (...); } else { (...); }</code>
<p class="uk-text-bold">Level 3:</p> Health: <code>Player.getHealth() // 100 is the max, an enemy deals 20 damage</code>
<p class="uk-text-bold">Level 4:</p> Turning: <code>Player.setDir("LEFT") // or "RIGHT" accordingly</code><br> Opening a chest: <code>Player.openChest() // if you are within range</code><br> Chest distance: <code>Player.getChestDistance()</code><br> "OR": <code>if(a || b) // or</code><br> "AND": <code>if(a && b) // and</code>
