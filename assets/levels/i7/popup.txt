The maximum health is 100, and the enemy damage is 20.

Since you would need two conditions, you must use elif (else if) statement.<br><br>

if (function() == "_"):<br>
    function()<br>
elif (function() == "_"):<br>
    function()<br>
else:<br>
    function()<br>
